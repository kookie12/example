print("test")

# make CNN only using numpy 
class CNN:
    def __init__(self, input_shape, output_shape):
        self.input_shape = input_shape
        self.output_shape = output_shape
        self.weights = []
        self.biases = []
        self.layers = []
        self.layers.append(self.input_shape)
        self.layers.append(self.output_shape)
        self.layers.append(self.weights)
        self.layers.append(self.biases)
        self.layers.append(self.input_shape)
        self.layers.append(self.output_shape)
        self.layers.append(self.weights)
        self.layers.append(self.biases)
        self.layers.append(self.input_shape)
        self.layers.append(self.output_shape)
        self.layers.append(self.weights)
        self.layers.append(self.biases)
        self.layers.append(self.input_shape)
        self.layers.append(self.output_shape)
        self.layers.append(self.weights)
        self.layers.append(self.biases)
        self.layers.append(self.input_shape)
        self.layers.append(self.output_shape)
        self.layers.append(self.weights)
        self.layers.append(self.biases)
        self.layers.append(self.input_shape)
        self.layers.append(self.output_shape)
        self.layers.append(self.weights)
        self.layers.append(self.biases)
        self.layers.append(self.input_shape)
        self.layers.append(self.output_shape)
        self.layers.append(self.weights)
        self.layers.append(self.biases)
        self.layers.append(self.input_shape)
        self.layers.append(self.output_shape)
    